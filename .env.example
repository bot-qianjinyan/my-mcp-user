# User MCP 示例环境变量
SECRET_KEY=dev-secret-change-me-in-production
API_HOST=127.0.0.1
API_PORT=8000
MCP_HOST=127.0.0.1
MCP_PORT=3001
API_BASE_URL=http://127.0.0.1:8000
DATABASE_URL=sqlite:///./data/users.db

# Gemini / Inspect（不要把真实 Key 提交进仓库）
# GOOGLE_API_KEY=
# 若出现 ClientConnectorCertificateError（常见于 Netskope），先跑：
#   ./scripts/build_ssl_certs.sh
# 再设置：
# SSL_CERT_FILE=.certs/combined-ca.pem
# REQUESTS_CA_BUNDLE=.certs/combined-ca.pem
